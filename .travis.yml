language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t axprin/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true axprin/docker-react npm run test -- --coverage